إطبع(0 == 0)
إطبع(صحيح == صحيح)
إطبع(عدم == عدم)
إطبع("السلام عليكم" == "السلام عليكم")
متغير قائمة = [0]
متغير نفس_قائمة = قائمة
متغير كائن = { الاسم: "يوسف" }
متغير نفس_كائن = كائن
متغير دالة_ = || {}
متغير نفس_دالة_ = دالة_
متغير ملف = إفتح("README.md"، "قراءة")
متغير نفس_ملف = ملف
إطبع(قائمة == نفس_قائمة)
إطبع(قائمة == [0])
إطبع(كائن == نفس_كائن)
إطبع(كائن == { الاسم: "يوسف" })
إطبع(دالة_ == نفس_دالة_)
إطبع(دالة_ == || {})
إطبع(إطبع == إطبع)
إطبع(إطبع == إفتح)
إطبع(ملف == نفس_ملف)
إطبع(ملف == إفتح("README.md"، "قراءة"))
