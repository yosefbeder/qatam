دالة شرّح(نص، البداية=0، النهاية=حجم(نص)) {
  متغير الناتج = ""
  متغير عداد = البداية
  طالما(عداد < النهاية) {
    الناتج += نص[عداد]
    عداد += 1
  }
  أرجع الناتج
}
متغير نص = "السلام عليكم"
إطبع(شرّح(نص))
إطبع(شرّح(نص، 1))
إطبع(شرّح(نص، 0، 6))
