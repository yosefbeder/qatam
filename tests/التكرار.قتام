مهمة البداية() {
    متغير الأسماء = [عدم، عدم، عدم]
    متغير القادم = 0
    طالما(القادم < 3) {
        إطبع("أدخل الاسم رقم " + القادم)
        الأسماء[القادم] = إمسح()
        القادم = القادم + 1
    }
    إطبع("تم ملاء مصفوفة الأسماء بنجاح 👍"، الأسماء)
}

البداية()
